import styled from 'styled-components'

export const StyledLabel = styled.label`
  font-size: ${({ theme }) => theme.tipographies.size.md};
  font-weight: 600;
  padding-left: 3px;
  font-size: 1.1rem;
`
